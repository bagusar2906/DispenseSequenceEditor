<UserControl x:Class="RMIDispenseSequenceEditor.Views.DispenseSequenceEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:RMIDispenseSequenceEditor.ViewModels"
             xmlns:local="clr-namespace:RMIDispenseSequenceEditor.Views"
             mc:Ignorable="d"
             x:Name="RootControl">

    <UserControl.DataContext>
        <vm:DispenseSequenceViewModel />
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <!-- ======================================================
             SOURCE INGREDIENT LIST (TOP)
        ======================================================= -->
        <Border Grid.Row="0"
                Padding="2"
                BorderBrush="#888"
                BorderThickness="1"
                Height="55"
                Width="800"
                CornerRadius="6"
                Margin="0 0 10 0"
                Background="#08FFFFFF">

            <StackPanel VerticalAlignment="Center">
                <ItemsControl ItemsSource="{Binding Ingredients}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"
                                       ItemWidth="120"
                                       ItemHeight="60" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:ChipControl
                                IsFromSource="True"
                                Text="{Binding}"
                                Margin="0 6 6 0">

                                <local:ChipControl.Style>
                                    <Style TargetType="local:ChipControl">
                                        <EventSetter Event="PreviewMouseMove"
                                                     Handler="SourceChip_PreviewMouseMove" />
                                    </Style>
                                </local:ChipControl.Style>

                            </local:ChipControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
            </StackPanel>
        </Border>


        <!-- ======================================================
             MIDDLE + RIGHT PANEL
        ======================================================= -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>


            <!-- ======================================================
                 DESTINATION SLOTS
            ======================================================= -->
            <ScrollViewer Grid.Column="0" Grid.Row="2"
                          Margin="25,0,0,0"
                          VerticalScrollBarVisibility="Auto">

                <ItemsControl ItemsSource="{Binding Slots}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>

                            <StackPanel Orientation="Vertical"
                                        Margin="0 10 0 0">

                                <!-- Row index + drop zone -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Row index -->
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding SlotIndex}"
                                               VerticalAlignment="Center"
                                               FontSize="14" />

                                    <!-- Drop area -->
                                    <Grid Grid.Column="1"
                                          Height="55"
                                          Margin="2 0 0 0"
                                          AllowDrop="True"
                                          Tag="{Binding SlotIndex}">

                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Dashed drop border -->
                                        <Rectangle Stroke="#888"
                                                   StrokeThickness="1"
                                                   StrokeDashArray="3 3"
                                                   Width="400"
                                                   Fill="#04FFFFFF"
                                                   RadiusX="6"
                                                   RadiusY="6" />

                                        <!-- Chips inside -->
                                        <ItemsControl Grid.Column="0"
                                                      ItemsSource="{Binding Chips}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"
                                                                VerticalAlignment="Center"
                                                                Margin="10 0" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>

                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <local:ChipControl
                                                        IsFromSource="False"
                                                        Text="{Binding}"

                                                        SlotIndex="{Binding DataContext.SlotIndex,
                                                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        RemoveCommand="{Binding DataContext.RemoveIngredientCommand, 
                                                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        MoveCommand="{Binding DataContext.MoveChipCommand,
                                                        RelativeSource={RelativeSource AncestorType=UserControl}}" >

                                                        <local:ChipControl.Style>
                                                            <Style TargetType="local:ChipControl">
                                                                <EventSetter Event="PreviewMouseMove"
                                                                             Handler="SlotChip_PreviewMouseMove" />
                                                            </Style>
                                                        </local:ChipControl.Style>

                                                    </local:ChipControl>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>

                                        </ItemsControl>

                                        <!-- Placeholder -->
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding PlaceholderText}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="#777"
                                                   FontStyle="Italic"
                                                   IsHitTestVisible="False" />

                                        <!-- Drag/drop events -->
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <EventSetter Event="DragEnter" Handler="RowPlaceholder_DragEnter" />
                                                <EventSetter Event="DragLeave" Handler="RowPlaceholder_DragLeave" />
                                                <EventSetter Event="DragOver" Handler="RowPlaceholder_DragOver" />
                                                <EventSetter Event="Drop" Handler="RowPlaceholder_Drop" />
                                            </Style>
                                        </Grid.Style>

                                    </Grid>

                                </Grid>


                                <!-- Step mode selection -->
                                <StackPanel Orientation="Horizontal"
                                            Margin="60,8,0,5">

                                    <RadioButton Content="Normal"
                                                 Margin="0,0,16,0"
                                                 GroupName="{Binding SlotIndex}"
                                                 IsChecked="{Binding IsNormal}" />

                                    <RadioButton Content="Batch"
                                                 Margin="0,0,16,0"
                                                 GroupName="{Binding SlotIndex}"
                                                 IsChecked="{Binding IsBatch}" />

                                    <RadioButton Content="Aliquoting"
                                                 GroupName="{Binding SlotIndex}"
                                                 IsChecked="{Binding IsAliquoting}" />
                                </StackPanel>

                            </StackPanel>

                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </ScrollViewer>

            <!-- Right panel -->
            <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,20,20,0">
                <TextBlock
                    Text="Preview"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Foreground="#777"
                    FontWeight="Bold"
                    FontSize="18"
                    FontStyle="Italic"
                    Margin="0,0,0,20"
                    />
                
                <local:IngredientsTableControl x:Name="IngredientsTable"
                                               DataContext="{Binding IngredientsTable}"
                                               />
            </StackPanel>

        </Grid>
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Content="Preview"
                    Command="{Binding PreviewCommand}"
                    IsEnabled="{Binding CanPreview}"
                    FontSize="14" Width="100" Margin="4,0,10,0" />
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    IsEnabled="{Binding CanCancel}"
                    FontSize="14" Width="100" Margin="4,0,10,0" />
            <Button Content="Submit"
                    Command="{Binding SubmitCommand}"
                    IsEnabled="{Binding CanSubmit}"
                    FontSize="14" Width="100" Margin="4,0,20,0" />
        </StackPanel>
    </Grid>
</UserControl>